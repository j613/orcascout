function brewTea() {
	let docPos = document.getElementById('beverage');
	docPos.insertAdjacentHTML('beforeend', '<div id="tea">TEA IMAGE WILL GO HERE</div>');
	// Make sure there is a div with the id 'beverage' at the bottom of the document
}

function brewCoffee() {
	window.location.href = "brew_coffee.html"; //This will be the error page
}